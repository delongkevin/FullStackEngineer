name: PokerApp
options:
  bundleIdPrefix: com.yourname
  deploymentTarget:
    iOS: "16.0"
targets:
  PokerApp:
    type: application
    platform: iOS
    sources: PokerApp
    settings:
      base:
        SWIFT_VERSION: 5.9
        PRODUCT_BUNDLE_IDENTIFIER: com.yourname.pokerapp
        INFOPLIST_FILE: PokerApp/Info.plist
